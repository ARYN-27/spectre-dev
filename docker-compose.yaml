version: '1.1'
services:
  jupyter-lab:
    #container_name: spectre-container
    build: .
    ports:
      - "8888:8888"
    volumes:
      - /home/aryn/spectre-dev/spectre-code:/spectre-code
      - /mnt/c/SPECTRE/Dataset:/spectre-code/dataset
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]